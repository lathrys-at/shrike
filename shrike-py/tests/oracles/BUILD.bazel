load("@rules_python//python:defs.bzl", "py_library")

package(default_testonly = True)

# Python ORACLE implementations: the byte-identity references the test suites
# compare the Rust runtime against.
py_library(
    name = "oracles",
    srcs = glob(["*.py"]),
    imports = [".."],
    visibility = ["//visibility:public"],
)
