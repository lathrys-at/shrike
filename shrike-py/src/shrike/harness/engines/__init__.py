"""Engine bindings the harness attaches to the kernel's service slots.

One subpackage per slot: ``embedding/`` (the embed runtime + in-process backends)
and ``recognition/`` (the OCR/ASR/describe facades). Mirrors the Rust workspace's
``engines/`` grouping.
"""
