"""The kernel-wrapping assembly: collection facade, index/derived views, the
harness that opens the kernel and attaches engines, plus the per-modality engine
bindings under ``engines/``.

Imports from ``platform/`` and the top-level contract (``schemas``, ``errors``)
below it; consumed by ``api/`` above it.
"""
